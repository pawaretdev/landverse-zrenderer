{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.multi"
  },
  "deploy": {
    "startCommand": "/zren/start.sh",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "production": {
      "autoDeploy": true,
      "branch": "main"
    },
    "staging": {
      "autoDeploy": true,
      "branch": "develop"
    }
  }
}
